# CampRank Agent 统一开发规范

本文档是所有后续 Codex Agent 必须遵守的统一规范。任何 Agent 开始开发前，必须先阅读项目根目录和当前任务相关文档，确认自己的工作边界，再进行实现。

## 开发前必须阅读

每个 Agent 开发前必须阅读：

- `README.md`
- `AGENTS.md`
- `docs/project_design.md`
- `docs/testing_strategy.md`
- 当前任务相关文档

示例：

- 做测试设计、验收、一键检查或最终汇报时，必须阅读 `docs/testing_strategy.md`。
- 做评论可信度、刷评过滤、差评识别时，必须阅读 `docs/review_quality_control.md`。
- 做价格、优惠、赠品比较时，必须阅读 `docs/platform_price_benefit_plan.md`。
- 做退货、退款、售后风险时，必须阅读 `docs/return_and_refund_risk_plan.md`。
- 做评分和推荐排序时，必须阅读 `docs/scoring_model.md`。
- 做多 Agent 衔接时，必须阅读 `docs/agent_workflow.md`。

## 必须遵守的原则

- 不重建项目。
- 不破坏已有目录结构。
- 不删除已有接口。
- 不跳过测试。
- 不为了测试通过而删除测试。
- 新增功能必须新增或更新对应测试。
- 没有测试结果，不算完成。
- 不允许只汇报“已测试”，必须汇报具体命令和结果。
- 不把所有逻辑写进一个文件。
- 不绕过登录、验证码或反爬限制。
- 不默认所有评论都是真实评论。
- 不默认最低价平台就是推荐平台。
- 不把爬虫当成项目核心，项目核心是消费决策。
- 不把商家宣传参数直接当成最终可信结论。
- 不把赠品标价等同现金价值。
- 不在缺失数据时强行给出高置信度推荐。
- 不在没有同款识别的情况下直接跨平台比价。

## 数据与采集合规要求

- 优先使用官方开放平台、联盟 API、公开数据源和用户可访问的公开页面。
- 公开网页解析只作为补充手段。
- 不绕过登录、验证码、访问频率限制或平台反爬机制。
- 第一版允许使用 mock/sample 数据跑通系统流程。
- 后续真实采集模块必须采用可插拔设计，避免业务逻辑依赖单一平台采集实现。

## 测试要求

后端改动必须通过：

```bash
cd backend && python -m pytest
```

前端改动必须通过：

```bash
cd frontend && npm run build
```

同时改前后端时，两个命令都必须通过。

新增功能的测试要求：

- 新增 API 必须写接口测试。
- 新增数据库模型必须写数据库测试。
- 新增评论分析函数必须写评论识别测试。
- 新增评分函数必须写评分排序测试。
- 新增比价函数必须写平台推荐测试。
- 新增退货风险函数必须写退货风险分层测试。
- 新增同款识别函数必须写同款归并和不归并测试。
- 具体分阶段测试要求必须遵守 `docs/testing_strategy.md`。

验收要求：

- 没有测试结果，不算完成。
- 新增功能必须新增或更新对应测试。
- 不允许删除测试来通过验收。
- 不允许只汇报“已测试”，必须汇报具体命令和结果。

如果因为环境问题无法运行测试，Agent 必须在最终汇报中说明：

- 尝试运行的命令。
- 失败原因。
- 是否是依赖、环境、路径或权限问题。
- 后续需要谁处理。

## 代码组织要求

- 后端应按职责拆分模块，例如 API、schema、database、services、analysis、scoring、tests。
- 前端应按职责拆分页面、组件、API 调用、类型定义和样式。
- 评论分析、评分模型、价格比较、退货风险不得全部堆在一个文件。
- 规则词典应独立维护，避免散落在业务逻辑中。
- 评分公式实现必须能追溯到 `docs/scoring_model.md`。
- 推荐解释生成必须保留可解释字段，不允许只返回一个总分。

## 业务判断要求

- 评论分析必须先计算可信度，再参与评分。
- 疑似刷评不能等同于真实好评。
- 有效差评必须区分质量问题、参数虚标、售后退货、物流和个人偏好。
- 同款识别必须在比价之前完成。
- 平台推荐必须同时考虑价格、优惠稳定性、赠品实用性、退货保障、售后信誉和数据置信度。
- 最低价平台可以单独展示，但不能直接等同 recommended_platform。
- 缺失字段必须影响 DataConfidenceScore。
- 严重风险标签必须在推荐解释中明确展示。

## 每个 Agent 最终汇报必须包含

- 本阶段完成内容。
- 修改/新增文件。
- 新增测试文件。
- 运行的测试命令。
- 测试结果。
- 未完成内容。
- 下一步建议。

## 当前阶段限制

Agent 2 阶段已允许创建后端、前端、数据库模型、sample/mock 数据、基础 API、基础页面和自动化测试。

Agent 2 阶段仍然禁止：

- 不实现真实爬虫或绕过平台限制。
- 不实现复杂评论真实性、刷评过滤、有效差评识别和小红书分析算法。
- 不实现完整价格优惠、退货风险、评分推荐算法。
- 不把 sample/mock 数据伪装成真实采集数据。
- 不默认最低价平台就是推荐平台。

后续 Agent 3、Agent 4、Agent 5 必须继续遵守 `docs/agent_workflow.md` 和 `docs/testing_strategy.md` 中的分工与验收要求。
